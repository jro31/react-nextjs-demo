// 'index.js' is the root file
// So if the url is 'my-domain.com' (with nothing afterwards) then 'index.js' will be loaded

const HomePage = () => {
  return <h1>The Home Page</h1>;
};

export default HomePage;
